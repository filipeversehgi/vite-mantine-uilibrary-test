import { Flex, Loader } from '@mantine/core'

function DataLoader() {
    return (
        <Flex justify="center" align="center" py={80}>
            <Loader />
        </Flex>
    )
}

export default DataLoader
