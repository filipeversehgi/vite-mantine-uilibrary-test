export type SettingsTemplateProps = {
    title: string
    children: React.ReactNode
}

export type SettingsTemplateSectionProps = {
    title: string
    description: string
    showDivider?: boolean
    children: React.ReactNode
}
